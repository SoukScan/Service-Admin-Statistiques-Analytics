name: CD – Admin Moderation Service (Dev)

on:
  workflow_dispatch:
  push:
    branches: [ "dev" ]

permissions:
  contents: read
  packages: read

jobs:
  deploy-dev:
    runs-on: ubuntu-latest

    steps:
      # --- 1. Checkout Sources
      - uses: actions/checkout@v4

      # --- 2. Set GHCR image env
      - name: Set GHCR image
        run: |
          echo "GHCR_IMAGE=ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/admin-moderation-backend:ci-${{ github.sha }}" >> $GITHUB_ENV

      # --- 3. Install Minikube
      - name: Setup Minikube
        uses: medyagh/setup-minikube@latest

      - name: Start Minikube
        run: |
          minikube start --wait all
          minikube addons enable ingress

      # --- 4. Auth GHCR
      - name: Login to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      # --- 5. Use Minikube Docker engine
      - name: Use Minikube Docker
        run: eval $(minikube docker-env)

      # --- 6. Create K8s secret for GHCR pulls
      - name: Create pull secret
        run: |
          kubectl create secret docker-registry ghcr-pull-secret \
            --docker-server=ghcr.io \
            --docker-username=${{ github.repository_owner }} \
            --docker-password=${{ secrets.GHCR_PAT }} \
            --docker-email=dev@example.com || true

      # --- 7. Pull latest image
      - name: Pull image
        run: docker pull ${{ env.GHCR_IMAGE }}

      # --- 8. Substitute SHA in deployment manifest
      - name: Replace GITHUB_SHA in deployment
        run: |
          sed -i "s|\${GITHUB_SHA}|${GITHUB_SHA}|g" k8s/deployment.yaml

      # --- 9. Deploy backend & wait for rollout
      - name: Deploy to Minikube
        run: |
          kubectl apply -f k8s/
          kubectl set image deployment/admin-moderation-backend admin-moderation-backend=${{ env.GHCR_IMAGE }}
          kubectl rollout status deployment/admin-moderation-backend

      # --- 10. Attendre totalement que le pod backend soit READY
      - name: Wait for app to be ready
        run: |
          kubectl wait --for=condition=ready pod -l app=admin-moderation-backend --timeout=180s || kubectl get pods -o wide

      # --- 11. Diagnostics si CrashLoopBackOff ou erreur
      - name: Show pod diagnostics if not running
        if: failure() # cette étape ne s'exécute que si la précédente a échoué
        run: |
          echo "DETECTED FAILURE: logs & describe below"
          kubectl get pods -o wide
          kubectl describe pod -l app=admin-moderation-backend
          kubectl logs -l app=admin-moderation-backend

      # -------- ELK Stack -----------------------------------
      - name: Install Helm
        run: |
          curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

      - name: Create observability namespace
        run: |
          kubectl create namespace observability --dry-run=client -o yaml | kubectl apply -f -

      - name: Add Helm repositories
        run: |
          helm repo add elastic https://helm.elastic.co
          helm repo add fluent https://fluent.github.io/helm-charts
          helm repo update

      - name: Install / Upgrade Elasticsearch
        run: |
          helm upgrade --install elasticsearch elastic/elasticsearch \
            --namespace observability \
            --set replicas=1 \
            --set minimumMasterNodes=1 \
            --set resources.requests.cpu=250m \
            --set resources.requests.memory=512Mi \
            --wait \
            --timeout 10m

      - name: Install / Upgrade Kibana
        run: |
          helm upgrade --install kibana elastic/kibana \
            --namespace observability \
            --wait \
            --timeout 10m

      - name: Install / Upgrade Fluent Bit
        run: |
          helm upgrade --install fluent-bit fluent/fluent-bit \
            --namespace observability \
            --set backend.type=es \
            --set backend.es.host=elasticsearch-master \
            --set backend.es.port=9200 \
            --wait \
            --timeout 5m

      - name: Show observability services
        run: |
          echo "::notice::ELK Stack déployé avec succès"
          echo "Namespace : observability"
          echo "Kibana Service : kubectl get svc -n observability kibana-kibana"

      # --- 12. Port-forward du service backend vers localhost en background
      - name: Forward K8s service to localhost
        run: |
          kubectl port-forward service/admin-moderation-backend-service 8080:8080 &
          echo $! > .pid
          sleep 10

      # --- 13. Vérifie la dispo HTTP AVANT scan ZAP (avec diagnostic + logs si KO)
      - name: Vérifie service HTTP
        run: |
          for i in {1..15}
          do
            curl -sf http://localhost:8080 && echo "Service OK" && exit 0
            echo "En attente du backend... ($i)"
            sleep 4
          done
          echo "Service NOT reachable, logs & describe backend pod :"
          kubectl get pods -o wide
          kubectl describe pod -l app=admin-moderation-backend
          kubectl logs -l app=admin-moderation-backend
          exit 1

      # --- 14. ZAP Baseline Scan (Kubernetes Service)
      - name: ZAP Baseline Scan (Kubernetes Service)
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: 'http://localhost:8080'
          cmd_options: -a -J report_json.json -w report_md.md -r report_html.html
        continue-on-error: true

      # --- 15. Cleanup port-forward tunnel
      - name: Kill port-forward
        if: always()
        run: kill $(cat .pid) || true
